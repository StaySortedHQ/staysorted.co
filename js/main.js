// Generated by CoffeeScript 1.10.0
(function() {
  $(function() {
    var count, darkColor, lightColor;
    lightColor = '#f2f7fe';
    darkColor = '#75aafe';
    count = 5;
    $('#countdown').html(count);
    return $('.chart').easyPieChart({
      barColor: lightColor,
      trackColor: darkColor,
      scaleLength: 0,
      size: 60,
      animate: {
        duration: 1000,
        enabled: true
      },
      onStop: function(from, to) {
        count -= 1;
        $('.chart').data('easyPieChart').disableAnimation();
        if (count < 0) {
          return window.location.href = "http://staysorted.com";
        } else {
          $('#countdown').html(count);
          $('.chart').data('easyPieChart').update(0);
          $('.chart').data('easyPieChart').enableAnimation();
          return $('.chart').data('easyPieChart').update(100);
        }
      }
    });
  });

}).call(this);
